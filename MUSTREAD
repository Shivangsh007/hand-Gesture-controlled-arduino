# hand-Gesture-controlled-arduino
The arduino and the python code controls only the 'RIGHT arrow','LEFT arrow','UP arrow' and 'DOWN arrow'
So according to it you can change the hotkeys of basically any program to make it work to your interest!

BUT BEFORE USING THE PYTHON CODE INSTALL pyautogui AND pyserial BY USING PIP COMMANDS
